### Lodash test frontend reactjs typescript

A React + TypeScript project to explore and practice Lodash utilities.  
This repository contains examples of how to use Lodash functions like `debounce`, `throttle`, `cloneDeep`, `isEmpty`, and more in a React + TypeScript environment.

---

## 🚀 Features

- ✅ React + TypeScript setup
- ✅ Usage of Lodash functions with TypeScript types
- ✅ Examples for:
  - `debounce` — prevent rapid API calls on input
  - `throttle` — limit scroll or resize events
  - `cloneDeep` — deep copy objects safely
  - `isEmpty` — check empty arrays or objects
  - `isEqual` — compare objects deeply
- ✅ Conditional rendering with Lodash utilities
- ✅ Fully typed with TypeScript

---

## 🛠 Installation

```bash
# Clone the repository
git clone https://github.com/your-username/lodash-test-frontend-react-ts.git
cd lodash-test-frontend-react-ts

# Install dependencies
npm install
# or
yarn install
```
